from pathlib import Path


def render_html(structured_resume_path: str | Path, template: str | None, output_path: str | Path) -> str:
    """Stub: render structured resume (JSON) to HTML using Jinja2 template."""
    return str(output_path)

